[project]
name = "memgraph-ha-connect"
version = "0.1.0"
description = "Connect to a Memgraph HA cluster through a coordinator and run a sanity check"
requires-python = ">=3.9"
dependencies = [
    "neo4j>=5.0",
]
